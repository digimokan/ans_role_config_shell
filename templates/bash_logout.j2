# vim: set filetype=bash:
# ANSIBLE-MANAGED CONFIGURATION FILE.
# SOURCED BY: THE USER'S LOGIN SHELL, ON EXIT, IF THE LOGIN SHELL IS BASH

################################################################################
# RUN POSIX-COMPATIBLE LOGOUT COMMANDS FOR A BASH LOGIN SHELL
################################################################################

{% for action in cfg_shell_logout_actions %}
{{ '# ' }}{{ action.descrip }}
{{ action.cmd }}

{% endfor %}
